<h1>Dashboard</h1>

<h2>Users:</h2>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= image_tag user.photo.url, alt: 'User Photo', class: 'user-photo' %>
      <%= link_to user.name, user_path(user) %>
      (Total Posts: <%= user.posts_counter %>)
    </li>
  <% end %>
</ul>
